# Prototype — Live URLs & Export

> The repo doesn't host the running prototype (that lives in Lovable). It hosts the *pointers* so a teammate can find it without asking you.

- **Lovable preview URL:**
- **Deployed prod URL (if any):**
- **Export instructions:** _how to pull the latest from Lovable into `src/` via the GitHub 2-way sync._

## Key flow screenshots
_Drop screenshots in [`screenshots/`](screenshots/) and link them here._
